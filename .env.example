# =============================================================================
# Sionna RT 無線電模擬系統 - 環境變數配置
# =============================================================================
# 複製此檔案為 .env 並根據需要調整設定
# cp env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# 資料庫配置 (PostgreSQL + PostGIS)
# -----------------------------------------------------------------------------
POSTGRES_USER=sionna_user
POSTGRES_PASSWORD=sionna_password_2025
POSTGRES_DB=sionna_db
POSTGRES_PORT=5433
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgis_db:5432/appdb

# -----------------------------------------------------------------------------
# 應用程式配置
# -----------------------------------------------------------------------------
# 後端 API 伺服器端口
BACKEND_PORT=8889

# 前端開發伺服器端口
FRONTEND_PORT=5174

# 應用程式環境 (development, staging, production)
APP_ENV=development

# -----------------------------------------------------------------------------
# JWT 和安全設定 (如果需要身份驗證)
# -----------------------------------------------------------------------------
# SECRET_KEY=your-super-secret-key-here
# JWT_ALGORITHM=HS256
# JWT_EXPIRATION_HOURS=24

# -----------------------------------------------------------------------------
# Sionna RT 模擬設定
# -----------------------------------------------------------------------------
# 預設場景
DEFAULT_SCENE=nycu

# 最大模擬設備數量
MAX_DEVICES=100

# 圖像渲染品質 (low, medium, high)
RENDER_QUALITY=medium

# 模擬快取 TTL (秒)
SIMULATION_CACHE_TTL=300

# -----------------------------------------------------------------------------
# 3D 渲染設定
# -----------------------------------------------------------------------------
# OpenGL 渲染後端 (osmesa, egl)
PYRENDER_BACKEND=osmesa

# 渲染圖像解析度
RENDER_WIDTH=1024
RENDER_HEIGHT=768

# 抗鋸齒採樣數
ANTIALIASING_SAMPLES=4

# -----------------------------------------------------------------------------
# 日誌設定
# -----------------------------------------------------------------------------
# 日誌等級 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 日誌格式 (json, text)
LOG_FORMAT=text

# 是否啟用詳細日誌
VERBOSE_LOGGING=false

# -----------------------------------------------------------------------------
# 開發設定
# -----------------------------------------------------------------------------
# 是否啟用 API 文檔
ENABLE_DOCS=true

# 是否啟用 CORS
ENABLE_CORS=true

# 允許的 CORS 來源
CORS_ORIGINS=http://localhost:5174,http://localhost:3000

# 是否啟用 API 限流
ENABLE_RATE_LIMITING=false

# -----------------------------------------------------------------------------
# 效能設定
# -----------------------------------------------------------------------------
# 資料庫連接池大小
DB_POOL_SIZE=10

# 資料庫連接池最大溢出
DB_POOL_MAX_OVERFLOW=20

# HTTP 請求超時 (秒)
HTTP_TIMEOUT=30

# 模擬運算超時 (秒)
SIMULATION_TIMEOUT=300

# -----------------------------------------------------------------------------
# 檔案存儲設定
# -----------------------------------------------------------------------------
# 上傳檔案最大大小 (MB)
MAX_UPLOAD_SIZE=100

# 場景檔案存儲路徑
SCENE_STORAGE_PATH=/app/static/scene

# 渲染結果存儲路徑
RENDER_OUTPUT_PATH=/app/static/rendered_images

# 臨時檔案存儲路徑
TEMP_STORAGE_PATH=/tmp/sionna

# -----------------------------------------------------------------------------
# 監控和健康檢查
# -----------------------------------------------------------------------------
# 健康檢查間隔 (秒)
HEALTH_CHECK_INTERVAL=30

# 是否啟用效能監控
ENABLE_METRICS=false

# 監控資料保留天數
METRICS_RETENTION_DAYS=7

# -----------------------------------------------------------------------------
# 外部服務設定 (如果需要)
# -----------------------------------------------------------------------------
# Redis 設定 (如果重新啟用)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=

# 郵件服務設定 (如果需要)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-password
# SMTP_TLS=true

# -----------------------------------------------------------------------------
# Docker 特定設定
# -----------------------------------------------------------------------------
# Docker 網路名稱
DOCKER_NETWORK=sionna-net

# Docker 卷名稱前綴
DOCKER_VOLUME_PREFIX=sionna

# 是否在開發模式下掛載本地目錄
MOUNT_LOCAL_VOLUMES=true 